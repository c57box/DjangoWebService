Set up the Django app:

Create a virtualenv
- `virtualenv venv`
Install packages
- `pip install requirements.txt`
Migration
- `cd exampleapp`
- `python manage.py migrate`
Run
- `python manage.py runserver`